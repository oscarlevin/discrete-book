\begin{tikzpicture}
  \foreach \x in {0,...,5} {
    \foreach \y in {0,...,5}{
    \fill (\x,\y) circle (5pt);
    }
  }
  \begin{scope}[shift={(-.5,-.5)}]
      \foreach \n in {1,...,5} {
      \draw[very thick] (\n,0) -- (\n,\n) -- (0,\n);
      }
  \end{scope}
\end{tikzpicture}