\documentclass[11pt]{exam}

\usepackage{amsmath, amssymb, multicol}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{chessboard}
\usepackage{tikz}

\def\d{\displaystyle}
\def\b{\mathbf}
\def\R{\mathbf{R}}
\def\Z{\mathbf{Z}}
\def\st{~:~}
\def\bar{\overline}
\def\inv{^{-1}}


\newcommand{\vtx}[2]{node[fill,circle,inner sep=0pt, minimum size=7pt,label=#1:#2]{}}
\newcommand{\va}[1]{\vtx{above}{#1}}
\newcommand{\vb}[1]{\vtx{below}{#1}}
\newcommand{\vr}[1]{\vtx{right}{#1}}
\newcommand{\vl}[1]{\vtx{left}{#1}}
\renewcommand{\v}{\vtx{above}{}}


%\pointname{pts}
\pointsinmargin
\marginpointname{pts}
\addpoints
\pagestyle{head}
%\printanswers

\firstpageheader{Math 228}{\bf Planar Graphs}{February 4, 2013}


\begin{document}

%space for name
%\noindent {\large\bf Name:} \underline{\hspace{2.5in}}
%\vskip 1em

We say a graph is {\em planar} if it is possible to draw it (on a plane) without any edges crossing (except at vertices).  

\begin{questions}
\question Which of the graphs below are planar?  You might need to redraw some.

\def\sb{.8}
\begin{center}
 \begin{tikzpicture}[scale=\sb]
  \draw[thick] (-1,0) \v -- (1,0)\v -- (1,2) \v -- (-1, 2) \v -- (-1,0) -- (1,2) ;
  \draw (0,-.5) node[below]{\large A};
\end{tikzpicture}
\hfill
 \begin{tikzpicture}[scale=\sb]
  \draw[thick] (-1,0) \v -- (1,0)\v -- (1,2) \v -- (-1, 2) \v -- (-1,0) -- (1,2) (-1,2) -- (1,0);
  \draw (0,-.5) node[below]{\large B};
\end{tikzpicture}
\hfill
 \begin{tikzpicture}[scale=\sb]
  \draw[thick] (-1,0) \v -- (1,0)\v -- (1,2) \v -- (-1, 2) \v -- (-1,0) -- (1,2) (-1,2) -- (1,0) (0,1) \v;
  \draw (0,-.5) node[below]{\large C};
\end{tikzpicture}
\hfill
\begin{tikzpicture}[scale=\sb+.05]
  \path (0,.9) +(18:1) coordinate (a);
  \path (0,.9) +(90:1) coordinate (b);
  \path (0,.9) +(162:1) coordinate (c);
  \path (0,.9) +(234:1) coordinate (d);
  \path (0,.9) +(306:1) coordinate (e);
  \draw[thick] (a) \v -- (b) \v -- (c) \v -- (d) \v -- (e) \v -- (a) -- (c) -- (e) -- (b) -- (d) -- (a);
  \draw (0,-.5) node[below]{\large D};
\end{tikzpicture}
\hfill
\begin{tikzpicture}[scale=\sb, xscale=1.5]
 \draw[thick] (-1, 0) \v -- (-.5,2) \v -- (0,0) \v -- (.5, 2) \v -- (1,0) \v -- (-.5,2) (.5,2) -- (-1,0);
 \draw (0,-.5) node[below]{\large E};
  \end{tikzpicture}

\end{center}

\vfill

\question When a graph is planar {\em and} drawn in a planar way (without edges crossing), it divides the plane into regions we call {\em faces}.  For each planar graph above, how many faces are there?  Be sure to count the {\em outside} region as a face as well.

\vfill
\vfill

\question Draw three {\em different} planar graphs, each having 4 faces.  Try to have a different number of vertices or edges for each graph (record these numbers).
\vfill
\vfill
\vfill

\newpage
\question Draw three {\em different} planar graphs, each having 5 vertices.  Try to have a different number of edges or faces for each graph (record these numbers).
\vfill
\vfill
\question Draw three {\em different} planar graphs, each having 6 edges. Try to have a different number of faces or vertices for each graph (record these numbers).
\vfill
\vfill
\question What is the relationship between the number vertices, edges and faces for planar graphs?  Use $V$, $E$, and $F$ to represent these numbers - find an equation relating them.
\vfill
\end{questions}
\end{document}


